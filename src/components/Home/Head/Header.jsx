import React from 'react';
import Navigation from '../../ShareComponents/Navigation/Navigation';

const Header = () => {
    return (
        <div>
            <Navigation/>
        </div>
    );
};

export default Header;